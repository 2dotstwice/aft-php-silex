<!-- Show error box if there are any errors. -->
{% if errors is defined and errors is not empty %}
    <div class="alert alert-danger">
        <!-- Loop over all errors and display each one. -->
        {% for error in errors %}
            <p>{{ error }}</p>
        {% endfor %}
    </div>
{% endif %}
